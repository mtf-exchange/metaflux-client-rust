# Rust build artefacts
/target

# Editor / backup
*.bk
*.swp
*.swo

# Local env
.env
.env.local

# macOS
.DS_Store

# Local build config (sccache etc). Never committed — CI runners lack the wrapper.
.cargo/
